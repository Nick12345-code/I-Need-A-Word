using UnityEngine;

public class AudioManager : MonoBehaviour
{
    // button select
    // button select (back)
    // if button is highlighted the currently playing music is changed somehow (instrument added, becomes muffled, etc)
    // correct letter, incorrect letter
    // win fanfare, lose fanfare

}
